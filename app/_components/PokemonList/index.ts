/*
  Index file used to avoid redundancy when importing components.
  So instead of importing the component like this: ../PokemonList/PokemonList
  we can import it like this: ../PokemonList
*/
import { default as PokemonList } from "./PokemonList";

export default PokemonList;
