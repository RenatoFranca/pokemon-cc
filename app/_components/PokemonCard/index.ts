/*
  Index file used to avoid redundancy when importing components.
  So instead of importing the component like this: ../PokemonCard/PokemonCard
  we can import it like this: ../PokemonCard
*/
import { default as PokemonCard } from "./PokemonCard";

export default PokemonCard;
